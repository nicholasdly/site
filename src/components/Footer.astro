---
import { formatDate } from "../lib/utils";

interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<footer class="mt-8 flex items-center justify-between text-gray-500">
  <span>— Nicholas Ly</span>
  <span>{formatDate(date)}</span>
</footer>
